# --- Secrets / env ---
.env

# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# --- 제3자 코드 · 모델 가중치 · 가상환경 (라이선스/용량 이유로 제외) ---
# deepB3P(라이선스 없음), ToxinPred3(GPL-3.0), 대용량 .pth/.pkl, ESMFold 캐시 등.
# 재현 방법은 README.md 참고 — clone + 모델 다운로드 + venv 생성.
vendor/
.cache/

# --- OS / IDE ---
.DS_Store
.claude/
